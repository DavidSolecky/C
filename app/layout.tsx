export const metadata = {
  title: "SOF-Track",
  description: "Sledovanie výroby",
};

export default function RootLayout({ children }) {
  return (
    <html lang="sk">
      <body>{children}</body>
    </html>
  );
}
